# CONFIG_DESC: FV-FV remap from map file — BfB digest across rank counts
TEST_NAME=imoab_map_target
TEST_MODE=bfb

: "${MOAB_BUILD:=/nfs/gce/projects/sigma/vijaysm/moab/build/e3sm-gnu}"
: "${MOAB_MESH_DIR:=/nfs/gce/projects/sigma/vijaysm/moab/MeshFiles}"

EXE="$MOAB_BUILD/test/earthsystem/parallel/imoab_map_target"

SRC_FLAG="-t"
TGT_FLAG="-m"
DEFAULT_SRC="$MOAB_MESH_DIR/unittest/srcWithSolnTag.h5m"
DEFAULT_TGT="$MOAB_MESH_DIR/unittest/outTri15_8.h5m"

ONLINE_FLAGS="--no_regression"
LOAD_FLAGS="--no_regression"
DIGEST_FLAG="--digest_prefix imoab_map_target_digest"
DIGEST_PREFIX="imoab_map_target_digest"
DIGEST_KERNELS="ocn"
DIGEST_TOLERANCE=1e-9

DEFAULT_RANKS="1 2 4"
